const Shimmer = () => {
  return (
    <h1
      style={{
        color: "red",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Loading........
    </h1>
  );
};

export default Shimmer;
